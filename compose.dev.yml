name: design-pattern-playground-dev

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: design_pattern_playground
      POSTGRES_USER: design_pattern_playground
      POSTGRES_PASSWORD: design_pattern_playground
    ports:
      - "${POSTGRES_HOST_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U design_pattern_playground -d design_pattern_playground"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
      target: dev
    # On Windows/MINGW, the named volume mounted at /workspace/.m2 is often root-owned.
    # Running as root by default avoids AccessDeniedException when Maven writes cache files.
    # Linux users can override with BACKEND_UID/BACKEND_GID (e.g. 1000:1000).
    user: "${BACKEND_UID:-0}:${BACKEND_GID:-0}"
    working_dir: /workspace
    command: ./mvnw spring-boot:run
    environment:
      HOME: /workspace
      MAVEN_USER_HOME: /workspace/.m2
      MAVEN_CONFIG: /workspace/.m2
      MAVEN_OPTS: -Dmaven.repo.local=/workspace/.m2/repository
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/design_pattern_playground
      SPRING_DATASOURCE_USERNAME: design_pattern_playground
      SPRING_DATASOURCE_PASSWORD: design_pattern_playground
      SPRING_H2_CONSOLE_ENABLED: "false"
      SPRING_SQL_INIT_MODE: always
      SPRING_SQL_INIT_DATA_LOCATIONS: classpath:sql/dev-default-admin.sql
      SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION: "true"
      APP_COOKIE_SECURE: "false"
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080"
    volumes:
      - ./Backend:/workspace
      - backend_m2:/workspace/.m2

  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
      target: dev
    working_dir: /app
    command: npm run dev -- --host 0.0.0.0 --port 5173
    environment:
      CHOKIDAR_USEPOLLING: "true"
      VITE_BACKEND_PROXY_TARGET: http://backend:8080
    depends_on:
      - backend
    ports:
      - "5173:5173"
    volumes:
      - ./Frontend:/app
      - frontend_node_modules:/app/node_modules

volumes:
  backend_m2:
  frontend_node_modules:
  postgres_dev_data:
